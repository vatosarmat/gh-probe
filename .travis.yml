language: node_js

node_js:
  - '12'

install:
  - yarn install

script:
  - yarn build
  - yarn test:coverage_report

deploy:
  provider: surge
  skip_cleanup: true
  project: ./build
  domain: gh-probe.surge.sh
